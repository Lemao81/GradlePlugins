buildscript {
    repositories {
        mavenCentral()
        flatDir {
            dirs '..\\plugin\\build\\libs'
        }
    }
    dependencies {
        classpath name: "plugin"
    }
}

apply plugin: 'archivaUpload'