buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath Plugins.kotlin
        classpath Plugins.androidBuild
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}