buildscript {
    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath Plugins.kotlin
        classpath Plugins.androidGradle
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}